# Private server runtime variables. Copy this file to .env.local for development.
# Never commit .env.local or expose these values in client-side code.
NOTION_API_KEY=
NOTION_DATABASE_ID=
# Optional when the database has one source; required for multiple sources.
NOTION_DATA_SOURCE_ID=
